# Chip

```django
{% chip value="django" deletable=True remove_on_delete=True %}
  Django
{% endchip %}
```

```js
import { mountChips } from ".../chip.js";
mountChips(document);
```

Событие `rui:delete` отменяемое.
