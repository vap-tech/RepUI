# Django integration

Serve `repui.css` and `repui.js` from static files. Custom controls submit through native or hidden named inputs. Server validation remains authoritative; RepUI renders the returned error through the documented field message contract.
