import { $$ } from '../core/dom.js'; import { placeFloating } from '../core/overlay.js';
let layer; function ensure(){if(layer)return layer;layer=document.createElement('div');layer.className='rui-tooltip';layer.setAttribute('role','tooltip');layer.hidden=true;document.body.append(layer);return layer;}
export function initTooltips(root=document){const tip=ensure();$$('[data-rui-tooltip]',root).forEach(el=>{let timer;const show=()=>{clearTimeout(timer);timer=setTimeout(()=>{tip.textContent=el.dataset.ruiTooltip;tip.hidden=false;placeFloating(el,tip,{side:el.dataset.ruiTooltipSide||'top',align:'center',gap:7});},Number(el.dataset.ruiTooltipDelay||350));};const hide=()=>{clearTimeout(timer);tip.hidden=true;};el.addEventListener('mouseenter',show);el.addEventListener('mouseleave',hide);el.addEventListener('focus',show);el.addEventListener('blur',hide);el.addEventListener('keydown',e=>{if(e.key==='Escape')hide();});});}
