export function initSearch(root=document){root.querySelectorAll('[data-rui-search]').forEach(el=>{if(el.dataset.ruiBound)return;el.dataset.ruiBound='1';const input=el.querySelector('input');const clear=el.querySelector('[data-rui-search-clear]');const sync=()=>{if(clear)clear.hidden=!input.value;el.dataset.hasValue=input.value?'true':'false'};input?.addEventListener('input',sync);clear?.addEventListener('click',()=>{input.value='';input.dispatchEvent(new Event('input',{bubbles:true}));input.focus()});sync();});}
