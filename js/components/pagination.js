export function initPagination(root=document){root.querySelectorAll('[data-rui-pagination]').forEach(pagination=>{if(pagination.dataset.ruiBound)return;pagination.dataset.ruiBound='1';pagination.addEventListener('click',event=>{const item=event.target.closest('[data-page]');if(!item||item.disabled)return;pagination.dispatchEvent(new CustomEvent('rui:pagechange',{bubbles:true,detail:{page:Number(item.dataset.page)}}));});});}
