4. Тесты Command Palette

Перенести как behavior specifications, но не сам legacy-код. Особенно полезны сценарии:

- disabled;
- loop;
- active/selected;
- refresh после HTMX;
- destroy;
- focus;
- ARIA.

Command Palette:
  оставить legacy до отдельной миграции

`NavigationMenu` и `Menubar` пока лучше оставить в `old`: они пригодятся, когда появится реальная задача на desktop navigation.
